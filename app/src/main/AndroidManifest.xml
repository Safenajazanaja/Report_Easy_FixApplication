<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reporteasyfixapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReportEasyFixApplication">
        <activity android:name=".presentation.test.TestActivity"></activity>
        <activity android:name=".presentation.call.CallActivity" />
        <activity android:name=".presentation.summarize.SumActivity" />
        <activity android:name=".presentation.profit.ProfitActivity" />
        <activity android:name=".presentation.completejob.CompleteActivity" />
        <activity android:name=".presentation.materialsreport.MaterialstActivity" />
        <activity android:name=".presentation.backlog.BacklogActivity" />
        <activity
            android:name=".presentation.login.LoginActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.main.MainActivity"
            android:screenOrientation="landscape" />
    </application>

</manifest>